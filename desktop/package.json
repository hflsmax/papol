{
  "name": "papol-desktop",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "tauri dev",
    "install:web": "npm ci --prefix ../frontend && npm ci --prefix ../viewer && npm ci --prefix ../board",
    "build:web": "node scripts/build-web.mjs",
    "test": "npm test --prefix ../frontend && npm test --prefix ../viewer && npm test --prefix ../board && cargo test --locked --manifest-path src-tauri/Cargo.toml",
    "check:native": "cargo fmt --check --manifest-path src-tauri/Cargo.toml && cargo clippy --locked --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings",
    "build": "APPLE_SIGNING_IDENTITY=${APPLE_SIGNING_IDENTITY:--} tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.5"
  }
}
