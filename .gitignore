# Runtime data — old local copies, not source (the Worker's own is under cloudflare/.wrangler)
papol.db
uploads/
board_uploads/

# Build artifacts
frontend/dist/
frontend/node_modules/
viewer/dist/
viewer/node_modules/
board/dist/
board/node_modules/
desktop/dist/
desktop/node_modules/
cloudflare/node_modules/
cloudflare/.wrangler/
cloudflare/site/
__pycache__/

nohup.out

# Local environment. .env holds settings and secrets; .direnv is direnv's
# cache of the nix shell.
.env
.env.macos-notarization
.credentials/
.direnv/
result
result-*

# Agent worktrees — scratch checkouts, not source
.claude/worktrees/
