{
  "name": "papol-analyzer",
  "private": true,
  "description": "The host's analyzer: takes a PDF, reads its references, markers, links and title block by rules (src/rules/), answers JSON. The Worker forwards addresses and stores rows; the CPU-heavy reading runs here.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs && git diff --exit-code -- dist",
    "start": "node dist/analyzer.js",
    "test": "node build.mjs && node --test test/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "unpdf": "^1.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^7.0.2"
  }
}
